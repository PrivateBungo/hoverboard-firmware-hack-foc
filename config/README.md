# config

Target configuration split introduced by the architecture cleanup plan.

- `user/` for user-tunable params.
- `feature/` for compile-time feature flags.
- `board/` for board/variant specific defaults.
- `control/` for control-loop constants and limits.
